---
description: Write or improve Rails RSpec coverage using the installed testing skill.
---
1. Treat any text supplied after `/rspec-agent` as the file path, class, feature scope, or testing target.
2. Invoke `@rspec-agent`.
3. Read the source and existing specs before writing new tests.
4. Match the spec type to the behavior under test and follow the repo's existing FactoryBot and RSpec conventions.
5. Keep test coverage aligned with `.windsurfrules`: test behavior, not implementation details.
6. When useful, recommend `/tdd-red-agent` before implementation or `/implementation-agent` after new failures are in place.
