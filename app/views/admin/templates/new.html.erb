<section class="space-y-6">
  <div>
    <p class="text-sm font-medium uppercase tracking-[0.2em] text-slate-500">Admin</p>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">New template</h1>
    <p class="mt-2 text-sm text-slate-600">Create a template configuration that can be selected in the resume builder.</p>
  </div>

  <%= render "form", template: @template %>
</section>
