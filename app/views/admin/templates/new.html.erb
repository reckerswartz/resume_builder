<section class="space-y-6">
  <%= render Ui::PageHeaderComponent.new(
    eyebrow: t("admin.templates.new.page_header.eyebrow"),
    title: t("admin.templates.new.page_header.title"),
    description: t("admin.templates.new.page_header.description"),
    density: :compact,
    actions: [
      { label: t("admin.templates.new.page_header.actions.back_to_templates"), path: admin_templates_path, style: :secondary }
    ]
  ) %>

  <%= render "form", template: @template %>
</section>
