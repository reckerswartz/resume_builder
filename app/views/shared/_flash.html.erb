<div id="flash" class="mb-6 space-y-3">
  <% flash.each do |type, message| %>
    <% next if message.blank? %>

    <div class="rounded-2xl px-4 py-3 text-sm font-medium shadow-sm <%= flash_class(type) %>">
      <%= message %>
    </div>
  <% end %>
</div>
