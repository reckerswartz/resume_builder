<section class="space-y-6">
  <div>
    <p class="text-sm font-medium uppercase tracking-[0.2em] text-slate-500">New resume</p>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">Start a resume</h1>
    <p class="mt-2 max-w-2xl text-sm text-slate-600">Choose a name and template. The builder will create starter sections for experience, education, skills, and projects.</p>
  </div>

  <%= render "form", resume: @resume %>
</section>
